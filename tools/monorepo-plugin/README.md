# monorepo-plugin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build monorepo-plugin` to build the library.

## Generators

For setup and usage instructions for the `setup-wp-env-sync` generator, see the root README section:

- [Setting up wp-env sync infrastructure](../../README.md#setting-up-wp-env-sync-infrastructure)
