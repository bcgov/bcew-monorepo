# CHEFS flow demo

Simple walkthrough: form + files, then one step at a time.

```bash
npx --yes serve tools/chefs-flow-demo -p 5177
```

**Save:** secrets → CHEFS check → lock → random embed ref → DB row → page keeps embed ref only
**Embed:** embed ref → find DB row → unlock in PHP → temporary pass → form on page
