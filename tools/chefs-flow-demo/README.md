# CHEFS flow demo

Simple walkthrough: form + files, then one step at a time.

```bash
npx --yes serve tools/chefs-flow-demo -p 5177
```

**Save:** secrets → CHEFS check → lock API key → DB row → block stores Form ID
**Embed:** Form ID → find DB row → unlock API key in PHP → temporary pass → form on page
