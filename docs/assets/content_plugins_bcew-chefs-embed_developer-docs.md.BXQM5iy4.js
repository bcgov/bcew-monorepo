import{_ as s,o as i,c as t,a6 as a}from"./chunks/framework.BfmC6wP_.js";const k=JSON.parse('{"title":"Developer docs","description":"","frontmatter":{},"headers":[],"relativePath":"content/plugins/bcew-chefs-embed/developer-docs.md","filePath":"content/plugins/bcew-chefs-embed/developer-docs.md"}'),n={name:"content/plugins/bcew-chefs-embed/developer-docs.md"};function r(o,e,d,l,h,c){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="developer-docs" tabindex="-1">Developer docs <a class="header-anchor" href="#developer-docs" aria-label="Permalink to “Developer docs”">​</a></h1><p>This guide explains how to develop and maintain the <code>bcew-chefs-embed</code> WordPress plugin.</p><h2 id="plugin-overview" tabindex="-1">Plugin overview <a class="header-anchor" href="#plugin-overview" aria-label="Permalink to “Plugin overview”">​</a></h2><p>The plugin provides the <code>bcew-chefs-embed/chefs-form</code> dynamic block. It stores a selected CHEFS Form ID in the block content, while the corresponding API key is stored securely in the plugin settings.</p><p>The block does not embed a form directly in saved post content. WordPress renders a mount point, and the frontend script requests a short-lived CHEFS token before loading the CHEFS form viewer.</p><h2 id="local-development" tabindex="-1">Local development <a class="header-anchor" href="#local-development" aria-label="Permalink to “Local development”">​</a></h2><p>Run commands from the monorepo root.</p><h3 id="install-dependencies" tabindex="-1">Install dependencies <a class="header-anchor" href="#install-dependencies" aria-label="Permalink to “Install dependencies”">​</a></h3><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bcew-chefs-embed:composer-install</span></span></code></pre></div><p>The Composer target installs the plugin&#39;s PHP development dependencies. The plugin requires PHP 7.4.</p><h3 id="build-and-start-wordpress" tabindex="-1">Build and start WordPress <a class="header-anchor" href="#build-and-start-wordpress" aria-label="Permalink to “Build and start WordPress”">​</a></h3><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bcew-chefs-embed:build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bcew-chefs-embed:wp-env-start</span></span></code></pre></div><p>The local site is available at <code>http://localhost:9012</code>. WordPress Admin is available at <code>http://localhost:9012/wp-admin</code>. The CHEFS settings page is available at <code>http://localhost:9012/wp-admin/admin.php?page=bcew-chefs-embed-settings</code>. The test environment uses port <code>9013</code> and is used by the Playwright targets. It is separate from the normal development site on port <code>9012</code>.</p><p>The <code>wp-env</code> lifecycle script activates the plugin after the environment starts. Build the assets again after changing block JavaScript, styles, or metadata.</p><h3 id="stop-or-reset-wordpress" tabindex="-1">Stop or reset WordPress <a class="header-anchor" href="#stop-or-reset-wordpress" aria-label="Permalink to “Stop or reset WordPress”">​</a></h3><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bcew-chefs-embed:wp-env-stop</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bcew-chefs-embed:wp-env-clean</span></span></code></pre></div><h2 id="block-architecture" tabindex="-1">Block architecture <a class="header-anchor" href="#block-architecture" aria-label="Permalink to “Block architecture”">​</a></h2><p>The block metadata is defined in <code>src/chefs-form/block.json</code>. Its registered name is <code>bcew-chefs-embed/chefs-form</code>, its category is <code>widgets</code>, and its editor icon is the WordPress <code>media-document</code> Dashicon.</p><p>The block has one persisted attribute:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">	&quot;formId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">		&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;string&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">		&quot;default&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The API key and runtime token are never block attributes.</p><h3 id="editor-flow" tabindex="-1">Editor flow <a class="header-anchor" href="#editor-flow" aria-label="Permalink to “Editor flow”">​</a></h3><ol><li><code>src/chefs-form/edit.js</code> requests the saved Form IDs from the <code>form-ids</code> REST route.</li><li>The Form IDs are displayed in the block sidebar.</li><li>Selecting a Form ID updates the persisted <code>formId</code> attribute.</li><li>If the selected Form ID is deleted by an administrator, the editor clears the stale selection.</li><li>The editor preview uses the runtime configuration endpoint and CHEFS viewer used by the published block.</li></ol><p>The editor preview is provided by <code>src/chefs-form/components/chefs-form-preview.js</code>. It uses the same runtime configuration endpoint as the published block and loads the CHEFS viewer through <code>src/chefs-form/utils/ensure-chefs-form-viewer.js</code>.</p><h3 id="frontend-flow" tabindex="-1">Frontend flow <a class="header-anchor" href="#frontend-flow" aria-label="Permalink to “Frontend flow”">​</a></h3><p><code>src/chefs-form/render.php</code> sanitizes the Form ID and renders the block wrapper with a <code>data-form-id</code> attribute. It renders an empty-state message when no Form ID is selected. It does not render an API key or token.</p><p><code>src/chefs-form/view.js</code> runs in the browser after the page loads. For each block, it:</p><ol><li>Finds the block wrapper and reads the Form ID from its <code>data-form-id</code> attribute.</li><li>Requests <code>/wp-json/bcew-chefs-embed/v1/embed-config?formId=&lt;form-id&gt;</code>. This request goes to WordPress, where the plugin retrieves the encrypted API key and exchanges it with CHEFS for a short-lived access token. The API key is never sent to the browser.</li><li>Loads the CHEFS form viewer web component from the CHEFS service if it is not already available on the page.</li><li>Creates the viewer inside the block&#39;s mount element and passes it the short-lived token and CHEFS base URL returned by WordPress. The visitor can then interact with the live form.</li><li>Listens for the viewer&#39;s <code>formio:submitDone</code> event. When submission succeeds, it removes the viewer and displays the configured confirmation message, or the generic success message when no custom message is configured.</li><li>Listens for the viewer&#39;s <code>formio:error</code> event. When CHEFS reports a submission error, it displays the error above the form and keeps the form available so the visitor can try again.</li></ol><p>If the configuration request or viewer script fails before the form loads, the block displays a load error in place of the form.</p><h2 id="rest-api" tabindex="-1">REST API <a class="header-anchor" href="#rest-api" aria-label="Permalink to “REST API”">​</a></h2><p>The plugin registers routes under <code>/wp-json/bcew-chefs-embed/v1</code>.</p><h3 id="get-saved-form-ids" tabindex="-1">Get saved Form IDs <a class="header-anchor" href="#get-saved-form-ids" aria-label="Permalink to “Get saved Form IDs”">​</a></h3><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>GET /wp-json/bcew-chefs-embed/v1/form-ids</span></span></code></pre></div><p>The block editor uses this route to populate its Form ID selector. It requires the current user to have the <code>edit_posts</code> capability and returns only saved Form IDs. Unauthorized requests receive a <code>403</code> response.</p><p>The route is registered in <code>bcew-chefs-embed.php</code> and handled by <code>bcew_chefs_embed_get_saved_form_ids()</code>.</p><h3 id="get-embed-configuration" tabindex="-1">Get embed configuration <a class="header-anchor" href="#get-embed-configuration" aria-label="Permalink to “Get embed configuration”">​</a></h3><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>GET /wp-json/bcew-chefs-embed/v1/embed-config?formId=&lt;form-id&gt;</span></span></code></pre></div><p>The editor preview and published frontend use this route. It is public because an anonymous visitor must be able to load a published form. The route:</p><ol><li>Looks up the configured Form ID.</li><li>Decrypts its stored API key on the server.</li><li>Sends a server-to-server Basic Authentication request to CHEFS.</li><li>Returns the short-lived token, CHEFS base URL, and configured confirmation message.</li></ol><p>The API key is not included in the response. Omitting the required <code>formId</code> parameter returns <code>400</code>; an unknown or undecryptable Form ID returns <code>404</code>. Failures contacting CHEFS or invalid CHEFS responses return an error response from the gateway/authentication path.</p><p>The route is registered and handled by <code>src/EmbedConfigController.php</code>.</p><h2 id="security-and-credentials" tabindex="-1">Security and credentials <a class="header-anchor" href="#security-and-credentials" aria-label="Permalink to “Security and credentials”">​</a></h2><p>The settings page requires the <code>manage_options</code> capability. Form save, deletion, and confirmation actions use WordPress admin-post handlers with nonce validation.</p><p>API keys are encrypted before they are stored by <code>src/CredentialsManager.php</code>. <code>src/Crypto.php</code> prefers libsodium and falls back to OpenSSL AES-256-GCM. The encryption key is derived from the WordPress authentication salts, so encrypted values depend on the WordPress installation&#39;s salts.</p><p>During form rendering, the server decrypts the API key and uses it only for the CHEFS token exchange. Basic Authentication is server-to-server. The API key remains server-side and is not included in block attributes, rendered HTML, frontend source, or REST responses. The short-lived token is intentionally returned by <code>embed-config</code> and the frontend assigns it to the CHEFS viewer&#39;s <code>auth-token</code> attribute so the browser can load the form. The token is not stored in the block&#39;s persisted attributes.</p><p>Do not commit API keys, add them to source code, or expose them in client-side code. Use the CHEFS settings page for configured credentials and use non-production credentials for local testing.</p><p>When moving a database between environments, remember that different WordPress salts cannot decrypt the same stored API-key ciphertext. A database-only migration may require the credentials to be entered again.</p><h2 id="testing-and-verification" tabindex="-1">Testing and verification <a class="header-anchor" href="#testing-and-verification" aria-label="Permalink to “Testing and verification”">​</a></h2><p>Run the build and test targets from the monorepo root:</p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bcew-chefs-embed:build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bcew-chefs-embed:test-integration</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bcew-chefs-embed:test-e2e</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bcew-chefs-embed:test-screenshot</span></span></code></pre></div><p>Use <code>test-screenshot-generate</code> when updating approved screenshot baselines. The browser tests use the test environment on port <code>9013</code>.</p><h3 id="manual-smoke-test" tabindex="-1">Manual smoke test <a class="header-anchor" href="#manual-smoke-test" aria-label="Permalink to “Manual smoke test”">​</a></h3><ol><li>Start the environment with <code>npx nx run bcew-chefs-embed:wp-env-start</code>.</li><li>Open <code>http://localhost:9012</code> and confirm WordPress loads.</li><li>Open the CHEFS settings page as an administrator and save a Form ID and API key.</li><li>Create a post and insert the CHEFS Form block.</li><li>Confirm the saved Form ID appears in the block sidebar.</li><li>Select the Form ID and confirm the editor preview loads when valid CHEFS credentials and network access are available.</li><li>Publish the post and open it as an anonymous visitor.</li><li>Confirm the form loads through <code>embed-config</code>.</li><li>Verify that the CHEFS API key is not present in the page source, frontend code, or REST response. The REST response may contain the short-lived token used to load the form.</li></ol><p>The local WordPress environment can be started without live CHEFS credentials, but loading and submitting a real form requires valid credentials and access to the CHEFS service.</p><h2 id="related-documentation" tabindex="-1">Related documentation <a class="header-anchor" href="#related-documentation" aria-label="Permalink to “Related documentation”">​</a></h2><p>For administrator setup and content-authoring instructions, see <a href="./user-docs">User docs</a>.</p><p>For the plugin overview and local environment summary, see <a href="./index">Plugin overview</a>.</p>`,57)])])}const g=s(n,[["render",r]]);export{k as __pageData,g as default};
